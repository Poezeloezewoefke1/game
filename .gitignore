# Godot import cache
.godot/
*.translation

# Intermediate test renders (the kept screenshots live in docs/screenshots/).
# The .import sidecars are Godot's, generated because the screenshots land inside the project.
build/*.png
build/*.png.import
build/linux/

# OS
.DS_Store
Thumbs.db
