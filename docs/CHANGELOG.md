# Changelog

The changelog lives at the repository root: [`../CHANGELOG.md`](../CHANGELOG.md).

It is kept in one place on purpose. Two copies drift, and a changelog that
disagrees with itself is worse than one that is slightly inconvenient to find.
